apiVersion: v1
kind: Service
metadata:
  name: transact-api
  namespace: transact
  labels:
    app.kubernetes.io/name: transact-api
    app.kubernetes.io/part-of: transact
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: transact-api
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
